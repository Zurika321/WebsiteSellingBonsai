@model WebsiteSellingBonsaiAPI.DTOS.View.BannerDTO

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Banner</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ImageOld)
        </dt>
        <dd class = "col-sm-10">
            @if (!string.IsNullOrEmpty(Model.ImageOld))
            {
             
                    <img src="@Url.Content("~/" + Model.ImageOld)" style="max-height:300px" alt="Product Image" class="img1" />

            }
            else
            {

                    <img src="/Data/noimage.png" style="max-height:300px" alt="Product Image" class="img1" />
           
            }
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="BAN_ID" />
        <input type="hidden" asp-for="ImageOld" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
